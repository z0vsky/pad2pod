[project]
name = "pad2pod"
version = "0.1.0"
description = "Bridges a WalkingPad C2 treadmill to ANT+ as a foot pod (SDM profile)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bleak>=3.0.2",
    "openant>=1.3.4",
    "ph4-walkingpad>=1.0.2",
]
